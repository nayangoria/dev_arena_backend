spring:
  datasource:
    url: ${postgresql://devarena_user:HZCJg3kAfrpAPjYe82zK1x6q6vCRnZuT@dpg-d87tddf7f7vs73dqrjgg-a.singapore-postgres.render.com/devarena_jcu8:jdbc:postgresql://localhost:5432/devarena}
    username: ${devarena_jcu8:postgres}
    password: ${HZCJg3kAfrpAPjYe82zK1x6q6vCRnZuT:postgrade}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:f1statsexplorersecretkey12345678901234567890}

admin:
  secret-key: ${ADMIN_SECRET_KEY:devArena@Admin#2026}